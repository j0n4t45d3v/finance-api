spring:
  application:
    name: finance

  profiles:
    active: ${PROFILE:dev}


server:
  servlet:
    context-path: /api

springdoc:
  swagger-ui:
    path: /doc
    filter: true
    operationsSorter: method
    tags-sorter: none
